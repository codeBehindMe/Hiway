from typing import Any, List, Tuple
import pandas as pd


class BridgeFinder:
    def __init__(self, bridges_df: pd.DataFrame) -> None:
        self.bridges_df = bridges_df

    def find_bridges_in_path(self, path: list) -> Any:
        pass
